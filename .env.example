# Optional — paths default to sensible values if unset

# Path to the Linear bridge script (default: ./scripts/create_linear_task_and_dispatch.py)
# LINEAR_BRIDGE_PATH=

# Path to psql binary (default: psql, found via PATH)
# PSQL_PATH=

# Postgres database name (default: agent_memory)
# POSTGRES_DB=
